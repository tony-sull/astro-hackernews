---
import Show from './Show.astro'

export interface Props<T> {
    each: Iterable<T>
}

const { each } = Astro.props
---

{each.map((value: unknown) => <Fragment set:html={Astro.slots.render('default', [value])} />)}

<Show when={!each.length}>
    <slot name="fallback" />
</Show>
